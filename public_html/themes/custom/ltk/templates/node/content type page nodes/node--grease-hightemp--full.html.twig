{#
/**
 * @file
 * Theme override to display a node.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
     Only "getter" methods (method names starting with "get", "has", or "is")
     and a few common methods such as "id" and "label" are available. Calling
     other methods (such as node.delete) will result in an exception.
 * - label: The title of the node.
 * - content: All node items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - author_picture: The node author user entity, rendered using the "compact"
 *   view mode.
 * - metadata: Metadata for this node.
 * - date: Themed creation date field.
 * - author_name: Themed author name field.
 * - url: Direct URL of the current node.
 * - display_submitted: Whether submission information should be displayed.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - node: The current template type (also known as a "theming hook").
 *   - node--type-[type]: The current node type. For example, if the node is an
 *     "Article" it would result in "node--type-article". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - node--view-mode-[view_mode]: The View Mode of the node; for example, a
 *     teaser would result in: "node--view-mode-teaser", and
 *     full: "node--view-mode-full".
 *   The following are controlled through the node publishing options.
 *   - node--promoted: Appears on nodes promoted to the front page.
 *   - node--sticky: Appears on nodes ordered above other non-sticky nodes in
 *     teaser listings.
 *   - node--unpublished: Appears on unpublished nodes visible only to site
 *     admins.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - content_attributes: Same as attributes, except applied to the main
 *   content tag that appears in the template.
 * - author_attributes: Same as attributes, except applied to the author of
 *   the node tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - teaser: Flag for the teaser state. Will be true if view_mode is 'teaser'.
 * - page: Flag for the full page state. Will be true if view_mode is 'full'.
 * - readmore: Flag for more state. Will be true if the teaser content of the
 *   node cannot hold the main body content.
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @ingroup templates
 *
 * @see template_preprocess_node()
 *
 * @todo Remove the id attribute (or make it a class), because if that gets
 *   rendered twice on a page this is invalid CSS for example: two lists
 *   in different view modes.
 */
#}
{%
  set classes = [
    node.bundle|clean_class,
    node.isPromoted() ? 'is-promoted',
    node.isSticky() ? 'is-sticky',
    not node.isPublished() ? 'is-unpublished',
    view_mode ? view_mode|clean_class,
    'clearfix',
  ]
%}
<article{{ attributes.addClass(classes) }}>

  {{ title_prefix }}
  {% if not page %}
    <h2{{ title_attributes }}>
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
    </h2>
  {% endif %}
  {{ title_suffix }}

  {% if display_submitted %}
    <footer>
      {{ author_picture }}
      <div{{ author_attributes.addClass('author') }}>
        {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}
        {{ metadata }}
      </div>
    </footer>
  {% endif %}

  <div{{ content_attributes.addClass('content') }}>
   <h1>{{ label }}</h1>

   {% if content.field_sub_title|render is not empty %}
       <span> {{ content.field_sub_title }} </span>
   {% endif %}

   {% if content.field_name_previous|render is not empty %}
       <div><span>Предыдущее название : </span> {{ content.field_name_previous }}</div>
   {% endif %}

      <div class="main-table col-lg-6 col-md-6 col-sm-6 col-xs-12">
        <div>
            {% if content.field_oil_brand|render is not empty %}
                <div class="mt-maslo-logo">{{ content.field_oil_brand }}</div>
            {% endif %}
        </div>

        <div class="mt-maslo-type"><b>Назначение : </b>Смазка</div>
        <div class="plain_text">Спецификации</div>

        {% if content.field_grease_nlgi_type|render is not empty %}
            <div class="mt-grease-nlgi"><span><b>Класс NLGI : </b></span> {{ content.field_grease_nlgi_type }}</div>
        {% endif %}

          {% if content.field_grease_din_type|render is not empty %}
            <div class="mt-grease-din"><span><b>Обозначение по DIN :</b></span> {{ content.field_grease_din_type }}</div>
          {% endif %}

        {% if content.field_grease_appl_type|render is not empty %}
            <div><span><b>Область применения : </b></span> {{ content.field_grease_appl_type }}</div>
        {% endif %}

        <div class="plain_text">Применение</div>

        {% if content.field_oil_apply_node|render is not empty %}
            <div>{{ content.field_oil_apply_node }}</div>
        {% endif %}

    </div>

      <div class="chars-table col-lg-6 col-md-6 col-sm-6 col-xs-12">
          <!--<div>
              <div>Показатель</div>
              <div>Метод</div>
              <div>{#{{ label }}#}</div>
          </div>-->

          {% if content.field_oil_base_oil|render is not empty %}
              <div><div>Базовое масло</div><div></div><div>{{ content.field_oil_base_oil }}</div></div>
          {% endif %}

          {% if content.field_grease_thickener|render is not empty %}
              <div><div>Загуститель</div><div></div><div>{{ content.field_grease_thickener }}</div></div>
          {% endif %}

          {% if content.field_oil_temp__low|render is not empty and content.field_oil_temp__high|render is not empty %}
              <div><div>Температурный диапазон : </div><div>{{ content.field_oil_temp__low }}</div><div>{{ content.field_oil_temp__high }}</div></div>
          {% endif %}

          {% if content.field_oil_temp__low|render is not empty and content.field_oil_temp__high|render is empty %}
              <div><div>Нижний температурный предел</div><div></div><div>{{ content.field_oil_temp__low }}</div></div>
          {% endif %}

          {% if content.field_oil_temp__high|render is not empty and content.field_oil_temp__low|render is empty %}
              <div><div>Верхний температурный предел</div><div></div><div>{{ content.field_oil_temp__high }}</div></div>
          {% endif %}

          {% if content.field_grease_base_oil_visc_40|render is not empty and content.field_base_oil_visc_method|render is not empty  %}
              <div><div>Вязкость базового масла при 40°C</div><div>{{ content.field_base_oil_visc_method }}</div><div>{{ content.field_grease_base_oil_visc_40 }}</div></div>
          {% endif %}

          {% if content.field_grease_base_oil_visc_100|render is not empty and content.field_base_oil_visc_method|render is not empty  %}
              <div><div>Вязкость базового масла при 100°C</div><div>{{ content.field_base_oil_visc_method }}</div><div>{{ content.field_grease_base_oil_visc_100 }}</div></div>
          {% endif %}

          {% if content.field_oil_density|render is not empty or content.field_oil_density_method|render is not empty  %}
              <div><div>Плотность</div><div>{{ content.field_oil_density_method }}</div><div>{{ content.field_oil_density }}</div></div>
          {% endif %}

          {% if content.field_grease_penetration|render is not empty %}
              <div><div>Пенетрация</div><div></div><div>{{ content.field_grease_penetration }}</div></div>
          {% endif %}

          {% if content.field_grease_speed_factor|render is not empty %}
              <div><div>Скоростной фактор</div><div></div><div>{{ content.field_grease_speed_factor }}</div></div>
          {% endif %}

          {% if content.field_grease_temp__drop|render is not empty or content.field_grease_temp__drop_method|render is not empty %}
              <div><div>Температура каплепадения</div><div>{{ content.field_grease_temp__drop_method }}</div><div>{{ content.field_grease_temp__drop }}</div></div>
          {% endif %}

          {% if content.field_oil_temp__pour|render is not empty or content.field_oil_temp__pour_method|render is not empty %}
              <div><div>Температура застывания</div><div>{{ content.field_oil_temp__pour_method }}</div><div>{{ content.field_oil_temp__pour }}</div></div>
          {% endif %}

          {% if content.field_oil_acid_number|render is not empty or content.field_oil_acid_number_method|render is not empty %}
              <div><div>Кислотное число</div><div>{{ content.field_oil_acid_number_method }}</div><div>{{ content.field_oil_acid_number }}</div></div>
          {% endif %}

          {% if content.field_oil_ash|render is not empty or content.field_oil_ash_method|render is not empty %}
              <div><div>Зольность</div><div>{{ content.field_oil_ash_method }}</div><div>{{ content.field_oil_ash }}</div></div>
          {% endif %}

      </div>

      <div class="text-description col-lg-7 col-md-7 col-sm-7 col-xs-12">
          {% if content.field_oil_description|render is not empty %}
              <div> {{ content.field_oil_description }} </div>
          {% endif %}
          {% if content.field_oil_spec|render is not empty %}
              <div><b>Особенности смазки :</b> {{ content.field_oil_spec }}</div>
          {% endif %}
      </div>

      <div class="pack-tds-table col-lg-5 col-md-5 col-sm-5 col-xs-12">
          {% if content.field_oil_packing|render is not empty %}
              <div class="mt-maslo-packing"><b>Варианты фасовки : </b> {{ content.field_oil_packing }}</div>
          {% endif %}

          {% if content.field_oil_dopuski|render is not empty %}
              <div class="mt-maslo-dopuski"><b>Применение : </b>{{ content.field_oil_dopuski }}

                  {% if content.field_oil_food|render is not empty %}
                      <br><b>Пищевой допуск : </b>{{ content.field_oil_food }}
                  {% endif %}

              </div>
          {% endif %}

          {% if content.field_oil_tds|render is not empty %}
              <div class="mt-maslo-tds">{{ content.field_oil_tds }}</div>
          {% endif %}
      </div>

      <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12"><span>Приведенные данные соответствуют состоянию техники на момент разработки. Возможны изменения. Данные не являются спецификацией на продукт и не освобождают
              пользователя от проведения собственных испытаний. Более подробная информация предоставляется по запросу.</span></div>

      <!--<div class="order-btn"><span>ЗАКАЗАТЬ</span></div>-->
  </div>

</article>
