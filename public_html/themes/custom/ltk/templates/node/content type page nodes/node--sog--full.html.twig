{#
/**
 * @file
 * Theme override to display a node.

TDS (Описание)
 	field_oil_tds 	File
Марка
 	field_oil_brand 	Entity reference
Металл применения СОЖ
 	field_sog_metal_type 	Entity reference
Обработка резанием алюминия
 	field_sog_adv_cons_cut_alum 	Text (plain)
Обработка резанием стали
 	field_sog_adv_cons_cut_steel 	Text (plain)
Общая обработка резанием
 	field_sog_adv_cons_cut_general 	Text (plain)
Одобрения
 	field_oil_dopuski 	Text (formatted, long, with summary)
Операция металлообработки
 	field_sog_operation 	Entity reference
Описание
 	field_oil_description 	Text (formatted, long, with summary)
Особенность
 	field_oil_spec 	Entity reference
Плотность при 20 °C
 	field_sog_density_20 	Text (plain)
Плотность при 20 °C
 - Метод 	field_sog_dencity_20_method 	Entity reference
Подзаголовок
 	field_sub_title 	Text (plain)
Показатель pH при 5%
 	field_sog_ph5 	Text (plain)
Показатель pH при 5% - Метод
 	field_sog_ph5_method 	Entity reference
Поправочный коэффициент рефрактометра
 	field_sog_refractometer 	Text (plain)
Сложная обработка резанием
 	field_sog_adv_cons_compl 	Text (plain)
Содержание минерального масла
 	field_sog_mineral_percent 	Number (integer)
Старое название
 	field_name_previous 	Text (plain)
Текст - Применение
 	field_oil_application 	Text (formatted, long, with summary)
Тип консистенции СОЖ
 	field_sog_cons_type 	Entity reference

 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
     Only "getter" methods (method names starting with "get", "has", or "is")
     and a few common methods such as "id" and "label" are available. Calling
     other methods (such as node.delete) will result in an exception.
 * - label: The title of the node.
 * - content: All node items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - author_picture: The node author user entity, rendered using the "compact"
 *   view mode.
 * - metadata: Metadata for this node.
 * - date: Themed creation date field.
 * - author_name: Themed author name field.
 * - url: Direct URL of the current node.
 * - display_submitted: Whether submission information should be displayed.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - node: The current template type (also known as a "theming hook").
 *   - node--type-[type]: The current node type. For example, if the node is an
 *     "Article" it would result in "node--type-article". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - node--view-mode-[view_mode]: The View Mode of the node; for example, a
 *     teaser would result in: "node--view-mode-teaser", and
 *     full: "node--view-mode-full".
 *   The following are controlled through the node publishing options.
 *   - node--promoted: Appears on nodes promoted to the front page.
 *   - node--sticky: Appears on nodes ordered above other non-sticky nodes in
 *     teaser listings.
 *   - node--unpublished: Appears on unpublished nodes visible only to site
 *     admins.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - content_attributes: Same as attributes, except applied to the main
 *   content tag that appears in the template.
 * - author_attributes: Same as attributes, except applied to the author of
 *   the node tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - teaser: Flag for the teaser state. Will be true if view_mode is 'teaser'.
 * - page: Flag for the full page state. Will be true if view_mode is 'full'.
 * - readmore: Flag for more state. Will be true if the teaser content of the
 *   node cannot hold the main body content.
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @ingroup templates
 *
 * @see template_preprocess_node()
 *
 * @todo Remove the id attribute (or make it a class), because if that gets
 *   rendered twice on a page this is invalid CSS for example: two lists
 *   in different view modes.
 */
#}
{%
  set classes = [
    node.bundle|clean_class,
    node.isPromoted() ? 'is-promoted',
    node.isSticky() ? 'is-sticky',
    not node.isPublished() ? 'is-unpublished',
    view_mode ? view_mode|clean_class,
    'clearfix',
  ]
%}
<article{{ attributes.addClass(classes) }}>

  {{ title_prefix }}
  {% if not page %}
    <h2{{ title_attributes }}>
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
    </h2>
  {% endif %}
  {{ title_suffix }}

  {% if display_submitted %}
    <footer>
      {{ author_picture }}
      <div{{ author_attributes.addClass('author') }}>
        {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}
        {{ metadata }}
      </div>
    </footer>
  {% endif %}

  <div{{ content_attributes.addClass('content') }}>
   <h1>{{ label }}</h1>

   {% if content.field_sub_title|render is not empty %}
       {{ content.field_sub_title }}
   {% endif %}

   {% if content.field_name_previous|render is not empty %}
       <div class="prev_name">Прежнее название : {{ content.field_name_previous }} </div>
   {% endif %}

    <div class="main-table col-lg-6 col-md-6 col-sm-6 col-xs-12">
        <div>
            {% if content.field_oil_brand|render is not empty %}
                <div class="mt-maslo-logo">{{ content.field_oil_brand }}</div>
            {% endif %}
        </div>

        <div class="mt-maslo-type"><b>Назначение : </b>Смазочно-охлаждающая жидкость</div>



        <div class="plain_text">Применение</div>


        {% if content.field_sog_metal_type|render is not empty %}
            <div class="mt-red-maslo-din"><span><b>Металлы : </b></span> {{ content.field_sog_metal_type }}</div>
        {% endif %}

        {% if content.field_sog_operation|render is not empty %}
            <div class="mt-red-maslo-din"><span><b>Операции : </b></span> {{ content.field_sog_operation }}</div>
        {% endif %}

    </div>

    <div class="chars-table col-lg-6 col-md-6 col-sm-6 col-xs-12">
        <div>
            <div>Показатель</div>
            <div>Метод</div>
            <div>{{ label }}</div>
        </div>

        {% if content.field_sog_mineral_percent|render is not empty %}
            <div>
                <div>Минерального масла, %</div>
                <div></div>
                <div>{{ content.field_sog_mineral_percent }}</div>
            </div>
        {% endif %}

        {% if content.field_sog_density_20|render is not empty %}
            <div>
                <div>Плотность</div>
                <div>{% if content.field_sog_density_20_method|render is not empty  %}{{ content.field_sog_density_20_method }}{% endif %}</div>
                <div>{{ content.field_sog_density_20 }}</div>
            </div>
        {% endif %}

        {% if content.field_sog_ph5|render is not empty %}
            <div>
                <div>pH при 5%</div>
                <div>{% if content.field_sog_ph5_method|render is not empty  %}{{ content.field_sog_ph5_method }}{% endif %}</div>
                    <div>{{ content.field_sog_ph5 }}</div>
            </div>
        {% endif %}

        {% if content.field_sog_refractometer|render is not empty %}
            <div>
                <div>Поправочный коэффициент рефрактометра</div>
                <div></div>
                    <div>{{ content.field_sog_refractometer }}</div>
            </div>
        {% endif %}

    </div>

    <div class="text-description col-lg-7 col-md-7 col-sm-7 col-xs-12">
          {% if content.field_oil_description|render is not empty %}
              {{ content.field_oil_description }}
          {% endif %}
          {% if content.field_oil_spec|render is not empty %}
              <span>Особенности СОЖ :</span> {{ content.field_oil_spec }}
          {% endif %}
          {% if content.field_oil_food|render is not empty %}
              <tr><td>Пищевой допуск : </td><td>{{ content.field_oil_food }}</td></tr>
          {% endif %}

      </div>

    <div class="pack-tds-table col-lg-5 col-md-5 col-sm-5 col-xs-12">

        <div>
            {% if content.field_oil_application|render is not empty %}
                {{ content.field_oil_application }}
            {% endif %}
        </div>

        <div class="plain_text"><b>Рекомендуемая концентрация продукта:</b></div>

        {% if content.field_sog_adv_cons_cut_general|render is not empty %}
            <div><span>Общее резание</span> {{ content.field_sog_adv_cons_cut_general }}</div>
        {% endif %}

        {% if content.field_sog_adv_cons_compl|render is not empty %}
            <div><span>Сложное резание</span> {{ content.field_sog_adv_cons_compl }}</div>
        {% endif %}

        {% if content.field_sog_adv_cons_cut_alum|render is not empty %}
            <div><span>Резание алюминия</span> {{ content.field_sog_adv_cons_cut_alum }}</div>
        {% endif %}

        {% if content.field_sog_adv_cons_cut_steel|render is not empty %}
            <div><span>Резание стали</span> {{ content.field_sog_adv_cons_cut_steel }}</div>
        {% endif %}


          {% if content.field_oil_packing|render is not empty %}
              <div class="mt-maslo-packing"><b>Варианты фасовки : </b> {{ content.field_oil_packing }}</div>
          {% endif %}

          {% if content.field_oil_dopuski|render is not empty %}
              <div class="mt-maslo-dopuski"><b>Допуски : </b>{{ content.field_oil_dopuski }}

                  {% if content.field_oil_food|render is not empty %}
                      <b>Пищевой допуск : </b>{{ content.field_oil_food }}
                  {% endif %}

              </div>
          {% endif %}

          {% if content.field_oil_tds|render is not empty %}
              <div class="mt-maslo-tds">{{ content.field_oil_tds }}</div>
          {% endif %}
      </div>

    <!--<div class="order-btn"><span>ЗАКАЗАТЬ</span></div>-->
  </div>

</article>
